# Pokedex_with_CLI
This is a school project which allows users to use it as if it were a Pokédex from the Pokémon franchise. 
To run the file, first use an IDE with python 3 installed, and then run setup.py once.
Once that is done, you can run main.py in the same IDE
